package com.mayank.syntax;

public class BasicQuestion {
public static void main(String[] args) {
	/*
	 * If computers understand only 0s and 1s, why do we even need programming languages? 
	 * What does �write once, run anywhere� actually mean technically?
	 * What problem does a �compiler� solve fundamentally? 
	 * Why does Java use bytecode instead of directly producing machine code?
	 *  What is the difference between JDK, JRE, and JVM conceptually (not definition)? 
	 *  Why must every Java program have a specific structure (public class + main method)?
	 *  Why do variables exist? What is their fundamental purpose? 
	 *  Why does Java enforce naming conventions (camelCase, PascalCase)?
	 *   If Jav a is compiled, why do people say it is interpreted? 
	 *   If JVM crashes, what happens to all running Java applications?
	 */
}
}
